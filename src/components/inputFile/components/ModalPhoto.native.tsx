import React, { FunctionComponent as FC } from 'react'
import { View, Text } from 'react-native'

interface Props {}

const ModalPhoto: FC<Props> = (props) => {
  return (
    <View>
      <Text>ModalPhoto</Text>
    </View>
  )
}

export default ModalPhoto
