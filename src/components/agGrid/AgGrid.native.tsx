import React, { FunctionComponent as FC } from 'react';
import { View, Text } from 'react-native';

interface Props {}

const AgGrid: FC<Props> = (props) => {
  return (
    <View>
      <Text>Nativo 333</Text>
    </View>
  );
};

export default AgGrid;
